using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyrScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {   
        //”ничтожение текущего обекта
        Destroy(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
